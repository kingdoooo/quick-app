.superpowers/
.venv/
.venv-bootstrap.stamp
__pycache__/
*.egg-info/
.pytest_cache/
cdk.out/
site-builder/config.ini
site-builder/contract/build/
# deploy_agentcore.py 构建时从 deployer/functions/ 复制进来，构建后删除；
# 提交它会留下一份会漂移的副本
site-builder/mcp/common.py
.DS_Store
site-builder/ONBOARDING.md

# 临时设计草稿与给外部审查的上下文（可能含真实资源 ID，不入库）
docs/design/

# backfill 的旧 policy 快照（回滚材料，含账号 ARN，不入库）
site-builder/scripts/backfill-old-policies.json*

# mattpocock skills 的本地 issue/spec（可能含真实资源 ID，不入库；见 docs/agents/issue-tracker.md）
.scratch/

# 账号信任边界闸门的基线：含单账号实测（principal 指纹、语句指纹、公钥指纹），
# 不随资产分发（ADR 0005 / 3c-final D1）。采用者首跑 `--update-baseline` 自己生成。
site-builder/scripts/account_trust_baseline.json
