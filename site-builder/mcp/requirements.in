# 范围声明（真源）。锁定产物是 requirements.txt，用 pip-compile 生成——
# 见那个文件头部的命令与理由。

# **上界钉在 <2**：本仓库全部测试是在 1.x（当前 1.26）上跑的，
# 而 mcp 2.0 是主版本跃迁（FastMCP / request_context 这些我们依赖的 API
# 面在主版本间没有兼容承诺）。锁定的目的是"部署的就是验证过的"，
# 让 pip-compile 自由解析到未验证的 2.0 与这个目的相反。
# 升到 2.x 需要单独一轮：改上界 → 重新生成 → 跑全部测试 → 真机冒烟。
mcp>=1.10,<2
boto3
